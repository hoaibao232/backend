<div class="mt-4">
    <h3>SỬA SẢN PHẨM</h3>

    <form method="POST" enctype = "multipart/form-data" action="/books/{{book._id}}?_method=PUT">
         <div class="form-group">
    <label for="name">Tên sản phẩm</label>
    <input type="text" class="form-control" value="{{book.name}}" id="name" name = "name">
  </div>

  <div class="form-group">
    <label for="description">Mô tả</label>
    <textarea class="form-control" id="description" name = "description">{{book.description}}</textarea>
  </div>

  <div class="form-group">
    <label for="videoID">Video</label>
    <input type="text" class="form-control" value="{{book.videoID}}"  id="videoID" name = "videoID">
  </div>

  <div class="form-group">
    <label for="price">Giá tiền</label>
    <input type="number" class="form-control" value="{{book.price}}" id="price" name = "price">
  </div>

  <div class="form-group">
    <label for="quantities">Số lượng sản phẩm</label>
    <input type="number" class="form-control" value="{{book.quantities}}" id="quantities" name = "quantities">
  </div>

  <div class="form-group">
    <label for="image">Hình ảnh</label>
    <input type="file" class="form-control-file" id="image" name = "image">
  </div>
  

  <button type="submit" class="btn btn-primary">Lưu</button>
</form>

</div>